using UnityEngine;

namespace Gameplay
{
    public class CheckPoint : MonoBehaviour
    {
        
        //TODO : Add a checkpoint system
        //TODO add a flag to check if the player has passed the checkpoint
        //TODO before the player passes the checkpoint, the flag cloth should be at the bottom of the flag pole
        //TODO after the player passes the checkpoint, the flag cloth should be at the top of the flag pole
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
